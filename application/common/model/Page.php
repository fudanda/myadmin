<?php

namespace app\common\model;

class Page extends Common
{
    protected $pk = 'page_id'; //主键

    /**
     * 设置自动写入时间戳
     * @var bool 是否设置
     */
    //protected $autoWriteTimestamp = false;

    /**
     * 设置软删除
     * @var bool 是否开启
     */
    //protected $deleteTime = false;
}
